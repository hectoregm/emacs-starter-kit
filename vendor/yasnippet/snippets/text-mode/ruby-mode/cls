#name : class ... end
#contributor : hitesh <hitesh.jasani@gmail.com>
# --
class ${1:$
         (let ((fn (capitalize (file-name-nondirectory
                                 (file-name-sans-extension
                                   (buffer-file-name))))))
           (cond
             (t (string-search-and-replace "_" "" fn))))}
  $0
end
